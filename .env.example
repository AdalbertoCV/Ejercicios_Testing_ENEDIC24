# Copia este archivo a proyectos_django/.env y rellena los valores.
# Nunca subas el .env real al repositorio.

DB_NAME=votantes
DB_USER=usuario_local
DB_PASSWORD=cambia_esto
DB_ROOT_PASSWORD=cambia_esto
DB_HOST=db

DEBUG=True
SECRET_KEY=genera-una-clave-con-django-secret-key
